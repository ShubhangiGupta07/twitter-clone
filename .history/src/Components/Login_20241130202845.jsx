import React from "react";
import { Card.Title } from "react-bootstrap";
import Card from "react-bootstrap/Card";

const Login = () => {
    return (<Card>
        <Card.Title>X Login</Card.Title>
    </Card>);
};

export default Login;