import React from "react";
import { CardTitle } from "react-bootstrap";
import Card from "react-bootstrap/Card";

const Login = () => {
    return (<Card>
        <CardTitle style={{display:}}>X Login</CardTitle>
    </Card>);
};

export default Login;